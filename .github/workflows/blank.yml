#<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Free Forex - Live Signals & Charts</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', sans-serif;
      background-color: #0f172a;
      color: #fff;
      text-align: center;
    }
    header {
      background-color: #1e293b;
      padding: 1rem;
      font-size: 1.5rem;
      font-weight: bold;
      color: #38bdf8;
    }
    .chart-container {
      width: 100%;
      height: 600px;
      margin-top: 10px;
    }
    footer {
      background: #1e293b;
      padding: 10px;
      font-size: 0.9rem;
      color: #94a3b8;
    }
    @media (max-width: 768px) {
      .chart-container {
        height: 500px;
      }
    }
  </style>
</head>
<body>
  <header>📈 Free Forex - Live TradingView Chart</header>

  <div class="chart-container">
    <!-- TradingView Widget BEGIN -->
    <iframe 
      src="https://s.tradingview.com/widgetembed/?frameElementId=tradingview_2bbd5&symbol=FX:EURUSD&interval=15&symboledit=1&saveimage=1&toolbarbg=f1f3f6&studies=[]&theme=dark&style=1&timezone=Etc%2FUTC&withdateranges=1&hide_side_toolbar=0&allow_symbol_change=1&details=1&calendar=1&hotlist=1&autosize=1"
      width="100%" height="100%" frameborder="0" allowfullscreen>
    </iframe>
    <!-- TradingView Widget END -->
  </div>

  <footer>
    🔗 Join our Telegram for free signals: <br>
    <a href="https://t.me/+uSCJaXMnQT43ZGI1" target="_blank" style="color:#38bdf8;">@FreeForex Telegram</a><br><br>
    &copy; 2025 Free Forex | Powered by TradingView
  </footer>
</body>
</html> This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.
